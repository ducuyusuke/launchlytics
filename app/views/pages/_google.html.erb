<div class="page-container">
  <div class="container-overview">
    <div class="overview">
      <div class="overview-charts">
        <div class="overview-first-box">
          <div class="overview-ctr-charts">
            <div class="overview-chart_1">
              <%# First chart %>
                <div id="calendar_chart60">

                  <div style="position: relative;top: 3px;width:500px;height:300px;left: 33px;">

                    <canvas id="calendarChart60" data-graph-data="<%=@graph_data.to_json%>"></canvas>

                  </div>

                  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

                  <script>
                    const ctx60 = document.getElementById('calendarChart60');

                    const gData60 = JSON.parse(ctx.dataset.graphData)[0]
                    // console.log("data", gData.datasets[0].data)
                    // console.log("label", gData.datasets[0].label)
                    // console.log("labels", gData.labels)


                    new Chart(ctx60, {
                      type: 'bar',
                      data: {

                        labels: gData10.labels,
                        datasets: [{
                          label: gData10.datasets[0].label,
                          data: gData10.datasets[0].data,

                          borderWidth: 1
                        }]
                      },
                      options: {
                        scales: {
                          y: {
                            beginAtZero: true
                          }
                        }
                      }
                    });
                  </script>
              </div>
            </div>
          </div>
          <div class="overview-conver-charts">
            <div class="lateral-chart">
              <div class="overview-chart_2">


                  <div style="position: relative; top: 83px; width: 480px; height: 266px;">
                    <canvas id="calendarChart61" data-graph-data="<%=@graph_polar%>"></canvas>
                  </div>

                  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

                  <script>
                    const ctx61 = document.getElementById('calendarChart61');
                    const data61 = JSON.parse(ctx.dataset.graphData)

                    new Chart(ctx61, {
                      type: 'polarArea',
                      data: {
                        labels: ['Sex: Famale', 'Sex: Male', 'Age: 25+', 'City: Rio de Janeiro', 'CTR: R$1,80'],
                        datasets: [{
                          label: '# of Votes',
                          data: [30, 55, 25, 30, 40],
                          borderWidth: 1
                        }]
                      },
                      options: {
                        scales: {
                          y: {
                            beginAtZero: true
                          }
                        }
                      }
                    });
                  </script>
              </div>
            </div>
          </div>
        </div>
        <div class="overview-second-box">
          <div class="botton-overview">
            <div class="overview-chart_3_0">

                  <div style="position: relative;top: 3px;width:500px;height:300px;left: 33px;">

                    <canvas id="calendarChart62" data-graph-data="<%=@graph_data.to_json%>"></canvas>

                  </div>

                  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

                  <script>
                    const ctx62 = document.getElementById('calendarChart62');

                    const gData62 = JSON.parse(ctx.dataset.graphData)[0]
                    // console.log("data", gData.datasets[0].data)
                    // console.log("label", gData.datasets[0].label)
                    // console.log("labels", gData.labels)


                    new Chart(ctx62, {
                      type: 'line',
                      data: {

                        labels: gData62.labels,
                        datasets: [{
                          label: gData62.datasets[0].label,
                          data: gData62.datasets[0].data,

                          borderWidth: 1
                        }]
                      },
                      options: {
                        scales: {
                          y: {
                            beginAtZero: true
                          }
                        }
                      }
                    });
                  </script>


            </div>

          </div>
          <div class="second-botton-overview">
            <div class="overview-chart_5">


                  <div style="width: 472px;height: 272px;position: relative;left: 91px;top: 14px;">
                    <canvas id="calendarChart63" data-graph-data="<%=@graph_data%>"></canvas>
                  </div>

                  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

                  <script>
                    const ctx63 = document.getElementById('calendarChart63');
                    const data63 = JSON.parse(ctx.dataset.graphData)

                    new Chart(ctx63, {
                            data: {
                                datasets: [{
                                    type: 'bar',
                                    label: 'Bar Dataset',
                                    data: [15, 5, 40, 60]
                                }, {
                                    type: 'line',
                                    label: 'Line Dataset',
                                    data: [5, 15, 25, 50],
                                }],
                                labels: ['January', 'February', 'March', 'April']
                            },
                          options: {
                            scales: {
                              y: {
                                beginAtZero: true
                              }
                            }
                          }
                        });
                  </script>

            </div>

            <div class="overview-chart_6">

                  <div style="width: 477px;height: 228px;position: relative;left: 1px;top: 12px;">
                    <canvas id="calendarChart64" data-graph-data="<%=@graph_data%>"></canvas>
                  </div>

                  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

                  <script>
                    const ctx64 = document.getElementById('calendarChart64');
                    const data64 = JSON.parse(ctx.dataset.graphData)

                    new Chart(ctx64, {
                      type: 'pie',
                      data: {
                        labels: ['Idade: 25+', 'Idade: 40+', 'Idade: 18+'],
                       datasets: [{
                          label: 'Age',
                          data: [300, 50, 100],
                          backgroundColor: [
                            'rgb(255, 99, 132)',
                            'rgb(54, 162, 235)',
                            'rgb(255, 205, 86)'
                          ],
                          hoverOffset: 4
                        }]
                      },
                      options: {
                        scales: {
                          y: {
                            beginAtZero: true
                          }
                        }
                      }
                    });
                  </script>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
