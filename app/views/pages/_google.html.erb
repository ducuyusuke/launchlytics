<div class="page-container">
  <div class="container-overview">
    <div class="overview">
      <div class="overview-charts">
        <div class="overview-first-box">
          <div class="overview-ctr-charts">
            <div class="overview-chart_1">
              <%# First chart %>
                <div id="calendar_chart">

                  <div style="position: relative;top: 3px;width:500px;height:300px;left: 33px;">
                    <canvas id="calendarChart15" data-graph-data="<%=@graph_data%>"></canvas>
                  </div>

                  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

                  <script>
                    const ctx15 = document.getElementById('calendarChart15');
                    const data15 = JSON.parse(ctx.dataset.graphData)

                    new Chart(ctx15, {
                      type: 'bar',
                      data: {
                        labels: ['Red', 'Blue', 'Yellow', 'Green', 'Purple', 'Orange'],
                        datasets: [{
                          label: '# of Votes',
                          data: data,
                          borderWidth: 1
                        }]
                      },
                      options: {
                        scales: {
                          y: {
                            beginAtZero: true
                          }
                        }
                      }
                    });
                  </script>
              </div>
            </div>
          </div>
          <div class="overview-conver-charts">
            <div class="lateral-chart">
              <div class="overview-chart_2">


                  <div style="position: relative; top: 83px; width: 480px; height: 266px;">
                    <canvas id="calendarChart16" data-graph-data="<%=@graph_polar%>"></canvas>
                  </div>

                  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

                  <script>
                    const ctx16 = document.getElementById('calendarChart16');
                    const data16 = JSON.parse(ctx.dataset.graphData)

                    new Chart(ctx16, {
                      type: 'polarArea',
                      data: {
                        labels: ['Age: 18-30', 'Sex: Male', 'Sex: Female', 'City: Brazil', 'Hour: 15h', 'CTR: R$0,62'],
                        datasets: [{
                          label: '# of Votes',
                          data: [30, 55, 25, 30, 40, 60],
                          borderWidth: 1
                        }]
                      },
                      options: {
                        scales: {
                          y: {
                            beginAtZero: true
                          }
                        }
                      }
                    });
                  </script>
              </div>
            </div>
          </div>
        </div>
        <div class="overview-second-box">
          <div class="botton-overview">
            <div class="overview-chart_3">

                  <div style="width: 296px; height: 260px; position: relative; top: 23px;">
                    <canvas id="calendarChart17" data-graph-data="<%=@graph_data%>"></canvas>
                  </div>

                  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

                  <script>
                    const ctx17 = document.getElementById('calendarChart17');
                    const data17 = JSON.parse(ctx.dataset.graphData)

                    new Chart(ctx17, {
                      type: 'line',
                      data: {
                        labels: ['Red', 'Blue', 'Yellow', 'Green', 'Purple', 'Orange'],
                        datasets: [{
                          label: '# of Votes',
                          data: data,
                          borderWidth: 1
                        }]
                      },
                      options: {
                        scales: {
                          y: {
                            beginAtZero: true
                          }
                        }
                      }
                    });
                  </script>


            </div>
            <div class="overview-chart_4">

                  <div style="width: 296px; height: 260px; position: relative; top: 23px;">
                    <canvas id="calendarChart18" data-graph-data="<%=@graph_data%>"></canvas>
                  </div>

                  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

                  <script>
                    const ctx18 = document.getElementById('calendarChart18');
                    const data18 = JSON.parse(ctx.dataset.graphData)

                    new Chart(ctx18, {
                      type: 'line',
                      data: {
                        labels: ['Red', 'Blue', 'Yellow', 'Green', 'Purple', 'Orange'],
                        datasets: [{
                          label: '# of Votes',
                          data: data,
                          borderWidth: 1
                        }]
                      },
                      options: {
                        scales: {
                          y: {
                            beginAtZero: true
                          }
                        }
                      }
                    });
                  </script>
            </div>
          </div>
          <div class="second-botton-overview">
            <div class="overview-chart_5">


                  <div style="width: 472px;height: 272px;position: relative;left: 91px;top: 14px;">
                    <canvas id="calendarChart19" data-graph-data="<%=@graph_data%>"></canvas>
                  </div>

                  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

                  <script>
                    const ctx19 = document.getElementById('calendarChart19');
                    const data19 = JSON.parse(ctx.dataset.graphData)

                    new Chart(ctx19, {
                            data: {
                                datasets: [{
                                    type: 'bar',
                                    label: 'Bar Dataset',
                                    data: [10, 20, 30, 40]
                                }, {
                                    type: 'line',
                                    label: 'Line Dataset',
                                    data: [5, 15, 25, 50],
                                }],
                                labels: ['January', 'February', 'March', 'April']
                            },
                          options: {
                            scales: {
                              y: {
                                beginAtZero: true
                              }
                            }
                          }
                        });
                  </script>

            </div>

            <div class="overview-chart_6">

                  <div style="width: 477px;height: 228px;position: relative;left: 1px;top: 12px;">
                    <canvas id="calendarChart20" data-graph-data="<%=@graph_data%>"></canvas>
                  </div>

                  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

                  <script>
                    const ctx20 = document.getElementById('calendarChart20');
                    const data20 = JSON.parse(ctx.dataset.graphData)

                    new Chart(ctx20, {
                      type: 'pie',
                      data: {
                        labels: ['Red', 'Blue', 'Yellow', 'Green', 'Purple', 'Orange'],
                        datasets: [{
                          label: '# of Votes',
                          data: data,
                          borderWidth: 1
                        }]
                      },
                      options: {
                        scales: {
                          y: {
                            beginAtZero: true
                          }
                        }
                      }
                    });
                  </script>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
